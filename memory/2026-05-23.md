# 2026-05-23 日志

## 渐进式重构（完成 Phase 1–7）

### Phase 1: 拆分 server.js ✅
- config/db.js 独立
- 路由拆分到 routes/

### Phase 2: Repository 层 ✅
- 4 个 repo 文件覆盖所有 SQL
- 路由层零残留 db.prepare（从 159 处归零）

### Phase 3: Service 层 ✅
- routes → services → repos 三层链
- 业务逻辑全部从路由移入 service

### Phase 4: 错误处理中间件 ✅
- middleware/errorHandler.js
- 自动区分 API(JSON) 和页面(HTML) 404/500 处理
- views/error.ejs 错误页

### Phase 5: API 返回统一 ✅
- utils/apiResponse.js：sendSuccess/sendFail
- 双兼容格式：{ success, ok, data, error }

### Phase 6: 表单验证 ✅
- 安装 Zod
- schemas/birdSchema.js + roomSchema.js
- 路由入口验证

### Phase 7: 模块化目录 ✅
- modules/birds / rooms / breeding / genetics
- 每个模块 index.js 聚合 routes/service/repo/schema
- server.js 从 modules/ 加载

### 待做（Phase 8–14）
登录权限 / 操作日志 / 数据备份 / 图片独立 / 摄像头升级 / genetics 独立 / 未来路线
# 2026-05-23 日志

## 渐进式重构（完成 Phase 1–7）

### Phase 1: 拆分 server.js ✅
- config/db.js 独立
- 路由拆分到 routes/

### Phase 2: Repository 层 ✅
- 4 个 repo 文件覆盖所有 SQL
- 路由层零残留 db.prepare（从 159 处归零）

### Phase 3: Service 层 ✅
- routes → services → repos 三层链
- 业务逻辑全部从路由移入 service

### Phase 4: 错误处理中间件 ✅
- middleware/errorHandler.js
- 自动区分 API(JSON) 和页面(HTML) 404/500 处理
- views/error.ejs 错误页

### Phase 5: API 返回统一 ✅
- utils/apiResponse.js：sendSuccess/sendFail
- 双兼容格式：{ success, ok, data, error }

### Phase 6: 表单验证 ✅
- 安装 Zod
- schemas/birdSchema.js + roomSchema.js
- 路由入口验证

### Phase 7: 模块化目录 ✅
- modules/birds / rooms / breeding / genetics
- 每个模块 index.js 聚合 routes/service/repo/schema
- server.js 从 modules/ 加载

### Phase 8: 登录权限系统 ✅
+ 三种角色：admin / keeper / viewer
+ 登录页 + session 持久化（文件存储）
+ 用户管理界面（仅 admin）
+ 顶栏登录状态显示 + 退出按钮

### Phase 9: 操作日志 ✅
+ activity_logs 表，自动记录关键操作
+ 日志查看页面（admin 后台，分页）

### Phase 10: 数据备份 ✅
+ 手动/自动备份（启动时 + 每天凌晨3点）
+ 智能清理（保留7天 + 每月1号）

### 访客模式改进
- 未登录可直接浏览所有页面（游客模式）
- 退出登录后跳转首页，而非登录页
- 访客看不到任何编辑/删除/新增按钮
- 登录按钮固定在导航栏最右侧

### 已修复 bug
- `birdRepo.updateBirdPhoto` 函数名错误→`updateImage`
- 图片上传路径双重 `/static/uploads/` 前缀
- 裁切上传 302 跳转导致 fetch 崩溃
- `Date.now()` session secret 导致重启全部掉登录

### 待做（Phase 11–14）
图片系统独立 / 摄像头升级 / genetics 独立 / 未来路线
